# Architecture

**Purpose:** Monorepo and service boundaries for NexLead.

- `frontend/` for Next.js UI and client services
- `backend/` for API, jobs, and integrations
- `shared/` for cross-package types and constants
- `database/` for schema, migrations, and seeds
