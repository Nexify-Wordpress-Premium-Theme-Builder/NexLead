# Security

**Purpose:** Auth, secrets, and data protection guidelines.

- JWT-based auth planned for backend APIs
- Secrets only via environment variables
- Workspace-scoped data access enforced at service layer
- Rate limiting and validation middleware prepared
