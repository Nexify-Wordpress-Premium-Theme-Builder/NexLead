# Decision Rules

**Purpose:** How architectural decisions are made.

- Favor simple MVP scope over premature abstraction
- Document breaking changes in `docs/technical`
- Keep shared package framework-agnostic
- Require review before adding new external dependencies
