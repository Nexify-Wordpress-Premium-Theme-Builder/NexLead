# Components

**Purpose:** Domain component organization rules.

- Group components by domain under `frontend/src/components`
- Keep business logic out of presentational components
- Use shared types from `shared/types`
- Prefer small focused files with kebab-case names
